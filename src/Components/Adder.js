import {React, useState} from 'react';

const Adder = props => {


    const renderer = ()=>{

    }



    return (
        <div>
            <button type="button" className="btn btn-success" onClick={renderer}>Adder</button>

        </div>
    );
};

export default Adder;
